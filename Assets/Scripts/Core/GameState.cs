namespace SnackAttack.Core
{
    public enum GameState
    {
        MainMenu,
        CharacterSelect,
        Gameplay,
        TreatAttack,
        Paused,
        Settings,
        GameOver,
        UploadAvatar,
        AvatarShowcase
    }
}
